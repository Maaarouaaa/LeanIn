/**
 * @deprecated Prefer `FlowProgress` from `@/components/ui/FlowProgress`.
 * Kept as a thin alias so older imports keep working during the transition.
 */
export {
  FlowProgress as ProgressTracker,
  type FlowStep,
} from "@/components/ui/FlowProgress";
